# run study

> 같이 있진 않지만, 같이 있는 느낌

친구들끼리 대화 없이 존재감만 공유하는 macOS 메뉴 막대 앱.

> **현재 상태 — UI 프로토타입**
> `.env` 없이 받으면 **목 데이터**로 돌아갑니다. 화면은 전부 동작하지만
> 실시간 공유는 Supabase 를 연결해야 켜집니다([아래](#supabase-연결)).
> Tauri(트레이·집중 모드) 쪽은 아직 실기기 검증 전입니다.

---

## 실행

```bash
npm install
npm run tauri:dev     # 개발 (맥에서만)
npm run dev           # 브라우저에서 UI만 확인 (트레이 없음)
```

### `tauri dev` 가 `cargo metadata ... No such file or directory` 로 죽는다면

Rust 가 안 깔린 겁니다. Tauri 는 Rust 로 빌드돼요.

```bash
xcode-select --install                                              # 이미 있으면 건너뜀
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh      # 기본값(1) 선택
source "$HOME/.cargo/env"                                           # 또는 터미널 새로 열기
cargo --version                                                     # 확인
npm run tauri:dev                                                   # 첫 빌드는 5~10분
```

배포용 빌드:

```bash
npm run tauri:build   # src-tauri/target/release/bundle/ 에 .app / .dmg
```

서명 없이 만든 앱은 다른 맥에서 "확인되지 않은 개발자" 경고가 뜹니다.
친구한테 줄 때는 **시스템 설정 → 개인정보 보호 및 보안 → 확인 없이 열기**를
누르라고 하거나, 터미널에서 `xattr -dr com.apple.quarantine "/Applications/run study.app"`.
공증까지 하려면 Apple Developer Program(연 $99)이 필요합니다.

---

## 지금 구현된 것

| | |
|---|---|
| 팝오버 | 메뉴 막대 아이콘 클릭 → 트레이 아래에 붙어서 열림, 포커스 잃으면 닫힘 |
| 격자 | 최대 3열, 인원 늘면 행 추가, 마지막 줄 가운데 정렬 |
| 셀 | 말풍선(2줄 고정·넘치면 말줄임) + 캐릭터 + 이름, 오프라인은 흐리게 |
| 상태 | 온라인(초록) / 집중 중(빨강) / 오프라인(회색) |
| 상태 메시지 | 최대 100자, 미리보기에서 실제 잘림 그대로, 만료 시간 선택 |
| 아이스브레이킹 질문 | 설정창 열 때마다 랜덤, **본인 화면에서만** 보임 |
| 배경 | 파스텔 6색 + 이미지 업로드 (말풍선이 반투명 흰 카드라 항상 읽힘) |
| 프로필 | 캐릭터 6종(구름·원·둥근네모·잎·하트·별) 또는 내 사진 (얼굴 모양대로 크롭, 드래그+확대로 위치 조절) |
| 상세 카드 | 남의 셀 클릭 → 상태 메시지 전문 + 이모지 반응 (내 셀·내 프로필은 상태 설정창) |
| 반응 | 고정 목록 없음. 이모지·카오모지·짧은 텍스트 아무거나 16자까지.<br>"내 반응" = 기본 ❤️👍😄 고정 + 그 뒤로 쓴 것 24개 누적(개별 삭제)<br>맥은 입력창에 포커스 두고 ⌃⌘Space 로 시스템 이모지 창 |
| 반응 표시 | 상태 메시지(말풍선) 위에 스티커로. 상태 메시지가 없으면 반응 자체가 없음 |
| 반응 달기 | 셀에 hover → ♡ 버튼 → 빠른 반응 6개 팔레트(＋ 는 자유 입력 창).<br>셀 더블클릭은 ❤️ 바로 달기 (단일 클릭과 220ms 로 구분) |
| 반응 +1 | 남이 단 반응을 누르면 나도 같은 반응. 카톡 공감이랑 같은 방식 |
| 반응 떼기 | 내가 단 반응을 누르면 작은 확인창(반응 + 쓰레기통). 쓰레기통 누르면 떼기, 바깥 누르면 취소.<br>쓰레기통은 기본 닫힘 → hover 시 열림. 남이 단 건 못 뗌 |
| 반응 주인 | 스티커 hover 시 "OO님이 반응했어요". 상세 카드에는 이모지별로 누가 달았는지 나열 |
| 타이머 | 집중 시작/정지, 집중 중 진입 시 macOS 집중 모드 연동 |
| 트레이 메뉴 | 수동 상태 지정, 상태 메시지, 설정, 종료 |
| 설정 | 자동 실행, 알림 끄기, 만료 기본값, 해제 규칙 |
| 온보딩 | 소개 → 파티 만들기/코드로 참여 |

## 상태 우선순위 (`src/lib/status.ts`)

```
1. 수동 오프라인   ← 항상 이김. 앱을 껐다 켜도 유지
2. 집중 중         ← 수동으로 켰거나, 타이머가 돌거나
3. 온라인          ← 기본값
```

해제는 **"들어온 문으로 나간다"**:

- 타이머로 진입한 집중 중 → 타이머 종료 시 자동 해제
- 수동으로 켠 집중 중 → 수동으로만 해제
- 수동 오프라인 → 수동으로만 해제

---

## 알림 끄기 (macOS 집중 모드)

macOS는 서드파티 앱이 집중 모드를 켜는 **공개 API를 제공하지 않습니다.**
다른 앱의 알림을 가로채거나 억제하는 것도 불가능합니다(Apple DTS 공식 답변).
그래서 유일하게 지원되는 경로인 **단축어**를 통해 우회합니다.

단축어 앱에서 두 개를 만들어 주세요:

| 단축어 이름 | 동작 |
|---|---|
| `run study 집중 켜기` | 집중 모드 설정 → 방해금지 켜기 |
| `run study 집중 끄기` | 집중 모드 설정 → 방해금지 끄기 |

이름 앞부분(`run study 집중`)은 설정에서 바꿀 수 있습니다.
앱은 `shortcuts run "<이름> 켜기"`를 실행합니다.

---

## Supabase 연결

`.env` 에 키가 없으면 **목 데이터**로 돌아가고, 있으면 진짜 백엔드에 붙습니다.
코드는 이미 양쪽 다 되어 있으니 아래 4단계만 하면 됩니다.

### 1. 프로젝트 만들고 익명 로그인 켜기

supabase.com 에서 프로젝트를 만든 뒤
**Authentication → Providers → Anonymous sign-ins** 를 켭니다. (기본은 꺼져 있음)

### 2. 스키마 올리기

**SQL Editor** 에 `supabase/schema.sql` 을 통째로 붙여넣고 실행합니다.
테이블 4개(`profile` · `party` · `party_member` · `reaction`), RLS 정책,
파티 생성·참여 함수가 한 번에 만들어집니다.

### 3. 키 넣기

```bash
cp .env.example .env
# Project Settings → API 에서 URL 과 anon key 복사
```

### 4. 끝

`npm run dev` 하면 자동으로 익명 로그인이 되고, 파티 만들기 화면이 뜹니다.
코드를 친구에게 보내면 그 코드로 들어옵니다.

### 로그인을 왜 안 만들었나

**익명 로그인도 진짜 `auth.uid()` 와 `authenticated` 역할을 받기 때문에
RLS 가 그대로 성립합니다.** 그래서 로그인 화면 없이도 남의 상태를 고치거나
남의 반응을 지우는 게 서버 차원에서 막힙니다.
(자체 UUID 를 쓰면 클라이언트가 자칭하는 값이라 RLS 가 성립하지 않습니다)

대신 익명 계정은 **기기에 묶입니다.** 앱 데이터를 지우거나 기기를 바꾸면
돌아올 수 없어요. 그래서 설정에 **"계정 연결"** 을 열어뒀습니다 —
`linkIdentity()` 로 GitHub 을 붙이면 영구 계정이 됩니다.

> ⚠️ 데스크톱 앱에서 OAuth 는 시스템 브라우저를 열고 커스텀 URL 스킴으로
> 돌아와야 합니다. 딥링크 설정 전까지는 `npm run dev`(브라우저)에서만 동작합니다.

### 무엇이 어디에 사는가

| | 저장 위치 | 이유 |
|---|---|---|
| 프로필, 상태 메시지, 반응 | **DB** | 남아야 함 |
| 온라인 여부, 집중 시작 시각 | **Realtime Presence** | 휘발성 |
| 내 설정, 수동 상태 | localStorage | 나만 보는 것 |

**⚠ 타이머는 절대 초 단위로 브로드캐스트하지 마세요.**
Supabase Presence 는 고빈도 업데이트용이 아니라고 공식 문서에 명시돼 있습니다.
`focus_started_at`(시작 시각) 하나만 보내고 흐르는 숫자는 각자 계산합니다.
이미 그렇게 짜여 있습니다.

상태 메시지를 바꾸면 `status_id` 가 새로 발급되고, 그 메시지에 달렸던
반응은 트리거가 정리합니다.

---

## 구조

```
src/
  types.ts              타입 + 팔레트 + 캐릭터 + 질문 목록
  lib/status.ts         상태 우선순위 규칙, 시간 포맷
  lib/presence.ts       공유 상태 인터페이스 + 팩토리
  lib/mockPresence.ts     └ 목 구현 (키 없을 때)
  lib/supabasePresence.ts └ 진짜 구현
  lib/supabase.ts       클라이언트 + 익명 로그인 + 계정 연결
  lib/tauri.ts          Tauri 브리지 (브라우저에서도 안전)
  lib/mock.ts           목 파티 데이터
  components/
    Face.tsx            캐릭터 얼굴 (사진이면 모양대로 크롭)
    MemberCell.tsx      격자 한 칸 (스티커·hover 반응 버튼)
    StatusModal.tsx     상태 설정 + 미리보기 + 배경
    ProfileModal.tsx    캐릭터 / 내 사진 + 크롭
    DetailCard.tsx      상세 + 반응 + 누가 달았는지
    EmojiPicker.tsx     자유 입력 + 내 반응
    ReactionPalette.tsx 빠른 반응 6개
    ConfirmDialog.tsx   반응 떼기 (쓰레기통)
    SettingsModal.tsx   설정
    Onboarding.tsx      파티 만들기 / 참여
supabase/
  schema.sql            테이블 + RLS + 파티 함수
src-tauri/
  src/lib.rs            트레이, 팝오버 위치, 집중 모드, 자동 숨김 토글
```

## 아직 안 된 것

- **실제 Supabase 프로젝트에서 검증 안 됨** — 코드와 스키마는 있지만
  실행해본 적 없음. 첫 연결 때 손볼 곳이 나올 수 있음
- 데스크톱 OAuth 딥링크 (계정 연결은 브라우저에서만)
- Tauri 쪽 컴파일 검증 (트레이·집중 모드·자동 숨김)
- 프로필 사진을 dataURL 로 DB 에 넣고 있음 — 커지면 Storage 로 옮길 것
- 노션식 커스텀 스티커(이미지 업로드)
- 리더보드, RunDev 연동 (Phase 2 이후)
- 앱 아이콘은 임시 생성본 — 실제 캐릭터로 교체 필요
